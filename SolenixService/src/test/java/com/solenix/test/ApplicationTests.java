package com.solenix.test;

import static org.junit.jupiter.api.Assertions.assertTrue;

import java.io.File;
import java.util.ArrayList;
import java.util.List;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import com.solenix.exception.InternalServerException;

@SpringBootTest
class ApplicationTests {
	
	/*
	 * @Autowired private JSLScriptGenerator jslFileGenerator;
	 */

	/*
	 * @Test void contextLoads() { }
	 */
	
	/*
	 * @Test public void testJSLFileGeneration() throws InternalServerException {
	 * List<ProgramTest> programTests = new ArrayList<ProgramTest>(); ProgramTest
	 * programTest = new ProgramTest(); programTest.setName("IDIGIO_LV.IIN_SDA_2");
	 * programTests.add(programTest);
	 * 
	 * String userHome = System.getProperty("user.home"); // Generate the JSL file
	 * jslFileGenerator.writeJSLFile(programTests,
	 * userHome+"/GymServices/test.jsl.data");
	 * 
	 * // Verify the file exists File jslFile = new
	 * File(userHome+"/GymServices/testScript.jsl"); assertTrue(jslFile.exists()); }
	 */


}
